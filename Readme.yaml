Go to the project directory and create a new conda environment with the required packages installed:

conda env create -f environment.yml

activate the environment using following command:
conda activate mlfd6


Execute the python code:
python3 2a.py


######################
 Execute the setup script to setup the environment :
bash setup.sh
